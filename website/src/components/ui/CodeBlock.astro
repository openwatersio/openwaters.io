---
interface Props {
  code: string;
  class?: string;
}
const { code, class: className } = Astro.props;
---

<div
  class:list={[
    "bg-navy-900 overflow-x-auto rounded-lg p-4 text-white",
    className,
  ]}
>
  <pre class="text-sm"><code>{code}</code></pre>
</div>
