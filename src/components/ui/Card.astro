---
import { cn } from '../../utils/cn';

interface Props {
  class?: string;
  hover?: boolean;
}

const { class: className, hover = false } = Astro.props;
---

<div class={cn(hover ? 'card-hover' : 'card', className)}>
  <slot />
</div>
