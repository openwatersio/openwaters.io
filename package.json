{
  "name": "openwaters-monorepo",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "website",
    "api"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w website\" \"npm run dev -w api\"",
    "dev:website": "npm run dev -w website",
    "dev:api": "npm run dev -w api",
    "build": "npm run build -w website"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "//": "TEMP: point @neaps/* at pkg.pr.new PR builds for the Cloudflare worker migration (tide-database#103, neaps#293). The override forces the transitive tide-database (under @neaps/api and neaps) to the PR build too. Revert to published versions once merged.",
  "overrides": {
    "@neaps/tide-database": "https://pkg.pr.new/@neaps/tide-database@103"
  },
  "dependencies": {
    "@neaps/api": "https://pkg.pr.new/@neaps/api@293",
    "@neaps/react": "^0.1.0",
    "@neaps/tide-database": "https://pkg.pr.new/@neaps/tide-database@103",
    "@neaps/tide-predictor": "^0.10.0",
    "neaps": "^0.7.0"
  }
}
